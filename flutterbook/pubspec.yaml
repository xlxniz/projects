name: hw3
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.11.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_calendar_carousel: ^2.0.2
  permission_handler: ^8.1.0
  path_provider: ^2.0.1
  scoped_model: ^1.1.0
  intl: ^0.17.0
  sqflite: ^1.2.0
  flutter_slidable: ^0.6.0
  image_picker: ^0.8.4
  cupertino_icons: ^1.0.2
  flutter_ringtone_player: ^3.1.1
  path:


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true


